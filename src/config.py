# Target
TARGET_COLUMN = "label"

# Numerical features
NUMERICAL_FEATURES = [
    # none for now
]

# Categorical features
CATEGORICAL_FEATURES = [
    # none for now
]

# Text features
TEXT_FEATURES = [
    "text"
]

# Excluded
EXCLUDED_COLUMNS = [
    # add if dataset has ID column
]

# Combined
ALL_FEATURES = TEXT_FEATURES
